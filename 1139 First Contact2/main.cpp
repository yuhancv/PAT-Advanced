#include <iostream>
#include"bits/stdc++.h"
using namespace std;

int n, m;
vector<vector<int>> graph;

int main() {
    scanf("%d%d", &n, &m);
    graph.resize(10010);

    for (int i = 0; i < m; ++i) {

    }

    return 0;
}
